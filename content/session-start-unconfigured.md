The project-context plugin is installed, but no API key is configured, so its
MCP tools are unavailable in this session. Do not call `recall_jitera_memory`
or `remember_jitera_memory`; the calls will fail.

If the user asks about project context, documentation, or shared memory, tell
them to connect this workspace by running:

    npx @jitera/connect login --install

or by configuring the plugin from the /plugin menu, then restarting the
assistant.
