Project context tools are configured for this workspace: project documentation,
source, and a shared project memory over MCP.

Before your first substantive answer in this session, and before planning or
implementing anything, call `recall_jitera_memory`. An empty result is normal
and is not an error. If the tools are missing from your tool list or a call
fails, say so and continue without stored context instead of guessing.

As decisions are made, constraints are discovered, or you are corrected, persist
them with `remember_jitera_memory`. Recall the entity first and re-send its full
attribute list, so you update it rather than creating a duplicate.
